node_modules/
dist/
storybook-static/
*.tsbuildinfo
.DS_Store
_to_delete/
.next/

# Playwright visual testing (tests/visual/) — committed baselines live in
# tests/visual/__screenshots__/, only run artifacts are ignored here.
test-results/
playwright-report/
tests/visual/__screenshots__/**/*-actual.png
tests/visual/__screenshots__/**/*-diff.png

# codegen package build output
packages/codegen/dist/

# codegen run result (read by the generate workflow; never committed)
codegen-result.json

# codegen visual-review result (read by the generate workflow; never committed)
visual-result.json
