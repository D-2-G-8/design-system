import type { Meta, StoryObj } from "@storybook/react";
import { Adviсes } from "./AdviEs";

const meta: Meta<typeof Adviсes> = {
  title: "Components/AdviEs",
  component: Adviсes,
  args: {},
};
export default meta;

type Story = StoryObj<typeof Adviсes>;

export const Default: Story = {};
