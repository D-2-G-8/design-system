import type { Meta, StoryObj } from "@storybook/react";
import { CheckmarkBadge02 } from "./CheckmarkBadge02";

const meta: Meta<typeof CheckmarkBadge02> = {
  title: "Components/CheckmarkBadge02",
  component: CheckmarkBadge02,
  args: {},
};
export default meta;

type Story = StoryObj<typeof CheckmarkBadge02>;

export const Default: Story = {};
